HOST=192.168.1.200
SSD_DIR=/path/to/ssd
# LOCAL DOMAIN (used by Caddy + dnsmasq for http://<service>.<DOMAIN>)
DOMAIN=home
# DASHBOARD
HOMEPAGE_ALLOWED_HOSTS=localhost:8087
HDD_DIR=/path/to/hdd
TZ=Etc/UTC
# BACKUP
BACKUP_LOCATION=/path/to/backups
SETTINGS_ENCRYPTION_KEY=changeme
# IMMICH
UPLOAD_LOCATION=/path/to/photos
DB_DATA_LOCATION=/path/to/immich/postgres
IMMICH_VERSION=release
DB_PASSWORD=changeme
DB_USERNAME=postgres
DB_DATABASE_NAME=immich
